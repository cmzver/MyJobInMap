# 🚀 Быстрый старт: новая форма заявок

**Версия:** 2.4.1  
**Дата:** 14 января 2026  
**Статус:** ✅ Готово к использованию

---

## ⚡ Быстрый старт (5 минут)

### 1️⃣ Запустить сервер

```bash
cd server
make run-server

# Или если нет make:
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8001
```

**Проверить:** http://localhost:8001/health

### 2️⃣ Запустить Portal

```bash
cd portal
npm install  # первый раз
npm run dev

# или npm run build для продакшена
```

**Проверить:** http://localhost:5173/portal

### 3️⃣ Создать заявку

1. Перейти на http://localhost:5173/portal/tasks/new
2. Заполнить адрес пошагово
3. Выбрать систему и тип неисправности
4. Заполнить остальные данные
5. Нажать "Создать заявку"

✅ **Готово!**

---

## 📦 Что изменилось

### Frontend (Portal)

✅ **Новые компоненты:**
- `AddressForm.tsx` — пошаговый ввод адреса
- `SystemSelector.tsx` — выбор системы
- `DefectTypeSelector.tsx` — выбор типа неисправности

✅ **Новый хук:**
- `useSettings.ts` — управление системными настройками

✅ **Переработана страница:**
- `TaskFormPage.tsx` — новый поток создания заявки

❌ **Удалено:**
- Система шаблонов (TEMPLATE_STORAGE_KEY)

### Backend (FastAPI)

✅ **Новые endpoints:**
```
GET  /api/admin/settings/defect-types/list        # Получить типы
POST /api/admin/settings/defect-types/add         # Добавить тип
DEL  /api/admin/settings/defect-types/{id}        # Удалить тип
```

✅ **Инициализация:**
- Добавлена настройка `defect_types` в БД
- 5 стандартных типов

---

## 🧪 Проверка работы

### 1. Загрузить типы неисправностей

```bash
curl http://localhost:8001/api/admin/settings/defect-types/list

# Ожидаемый ответ:
[
  {"id": "1", "name": "Замена фильтра", "description": null},
  {"id": "2", "name": "Замена счётчика", "description": null},
  ...
]
```

### 2. Открыть форму создания

**URL:** http://localhost:5173/portal/tasks/new

### 3. Проверить консоль браузера

```javascript
// F12 → Console
// Ошибок не должно быть
```

### 4. Проверить логи сервера

```bash
# Terminal с сервером
# Ищем "defect_types" и "settings initialized"
```

---

## 🔧 Конфигурация

### Типы неисправностей (стандартные)

Хранятся в: `server/app/models/settings.py` → `init_default_settings()`

```python
{
    "key": "defect_types",
    "value": '[{"id": "1", "name": "Замена фильтра", ...}]',
    "value_type": "json",
    "group": "interface",
    "label": "Типы неисправностей"
}
```

**Добавить новый тип по умолчанию:**
1. Отредактировать `defect_types` value в БД
2. Или добавить через API:
   ```bash
   POST /api/admin/settings/defect-types/add
   {"name": "Новый тип"}
   ```

---

## 📚 Документация

### Подробная информация:
- `IMPLEMENTATION.md` — полная архитектура
- `IMPLEMENTATION_SUMMARY.md` — краткое резюме
- `TESTING_GUIDE.md` — сценарии тестирования

### API документация:
- http://localhost:8001/docs (Swagger UI)
- http://localhost:8001/redoc (ReDoc)

---

## ⚙️ Решение проблем

### Проблема: "defect_types not found"

**Решение:**
```bash
# 1. Перезапустить сервер
make run-server

# 2. Или инициализировать вручную
python
>>> from app.models import init_default_settings
>>> from app.models import get_db
>>> db = next(get_db())
>>> init_default_settings(db)
```

### Проблема: Форма не загружается

**Решение:**
```bash
# 1. Проверить консоль браузера (F12 → Console)
# 2. Проверить Network tab
# 3. Убедиться что сервер запущен
# 4. Очистить кеш браузера (Ctrl+Shift+Del)
```

### Проблема: Типы неисправностей не загружаются

**Решение:**
```bash
# Проверить в браузере:
curl http://localhost:8001/api/admin/settings/defect-types/list

# Если 404 → сервер не запущен
# Если пусто → настройка не инициализирована
```

---

## 📊 Поток данных

```
┌─────────────────────────────────────────┐
│ Portal (React)                          │
│ TaskFormPage.tsx                        │
└────────────────┬────────────────────────┘
                 │
          ┌──────┴───────┐
          ↓              ↓
    ┌──────────────┐  ┌──────────────────┐
    │ AddressForm  │  │ DefectTypeSelector│
    └──────────────┘  └──────────────────┘
          │                   │
          └─────────┬─────────┘
                    ↓
        ┌─────────────────────────┐
        │ API calls to Backend    │
        │ /api/addresses/*        │
        │ /api/admin/settings/*   │
        └────────────┬────────────┘
                     ↓
        ┌─────────────────────────┐
        │ FastAPI Server (8001)   │
        │ - system_settings.py    │
        │ - addresses.py          │
        └────────────┬────────────┘
                     ↓
        ┌─────────────────────────┐
        │ SQLite Database         │
        │ - system_settings       │
        │ - addresses             │
        └─────────────────────────┘
```

---

## 🎯 Основные endpoint'ы

### Типы неисправностей

| Метод | Endpoint | Описание | Доступ |
|-------|----------|---------|--------|
| GET | `/api/admin/settings/defect-types/list` | Получить все типы | Все |
| POST | `/api/admin/settings/defect-types/add` | Добавить тип | Админ |
| DEL | `/api/admin/settings/defect-types/{id}` | Удалить тип | Админ |

### Адреса

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/api/addresses/autocomplete/cities` | Города |
| GET | `/api/addresses/autocomplete/streets` | Улицы |
| GET | `/api/addresses/autocomplete/buildings` | Дома |
| GET | `/api/addresses/{id}/systems` | Системы адреса |

---

## 📝 Версионирование

**Текущая версия:** 2.4.1

**Изменения в 2.4.1:**
- ✅ Добавлена новая форма заявок
- ✅ Удалена система шаблонов
- ✅ Добавлены типы неисправностей
- ✅ Добавлена интеграция с адресами

**Обновить версию:**
```bash
# 1. server/app/config.py
API_VERSION = "2.4.1"

# 2. server/app/__init__.py
__version__ = "2.4.1"

# 3. CHANGELOG.md
# Добавить запись
```

---

## 🆘 Поддержка

### Документы:
- [IMPLEMENTATION.md](./IMPLEMENTATION.md) — архитектура
- [TESTING_GUIDE.md](./TESTING_GUIDE.md) — тестирование
- [AGENTS.md](./AGENTS.md) — для AI

### Контакты:
- 📧 Див. AGENTS.md для инструкций AI
- 🐛 Ошибки в GitHub Issues
- 💬 Вопросы в Discussions

---

**Последнее обновление:** 14 января 2026  
**Статус:** ✅ Production Ready  
**Версия:** 2.4.1
