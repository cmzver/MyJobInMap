# Реализация новой формы заявок с пошаговым вводом адреса

**Версия:** 2.4.1  
**Дата:** 14 января 2026  
**Статус:** ✅ В процессе реализации

## Обзор

Переделана форма добавления/редактирования заявок в Portal с новой логикой:
- ❌ **Удалены**: система шаблонов
- ✅ **Добавлены**: пошаговый ввод адреса с автодополнением по городам, улицам, домам
- ✅ **Добавлены**: выбор системы обслуживания
- ✅ **Добавлены**: выбор типа неисправности (управляется через системные настройки)
- ✅ **Улучшено**: интерфейс стал более логичным и структурированным

---

## 📋 Компоненты Portal (React)

### 1. `AddressForm.tsx` (новый компонент)
**Путь:** `portal/src/components/AddressForm.tsx`

**Функциональность:**
- Пошаговый ввод адреса: Город → Улица → Дом → Корпус → Подъезд
- Автодополнение на каждом шаге через API
- Валидация обязательных полей
- Отображение полного адреса в конце

**API интеграция:**
- `addressesApi.autocompleteCities()` — список городов
- `addressesApi.autocompleteStreets()` — список улиц по городу
- `addressesApi.autocompleteBuildings()` — список домов по улице
- Корпусы и подъезды загружаются при выборе дома (из API Address)

### 2. `SystemSelector.tsx` (новый компонент)
**Путь:** `portal/src/components/SystemSelector.tsx`

**Функциональность:**
- Выбор системы обслуживания (видеонаблюдение, домофония, АППЗ и т.д.)
- Загрузка систем для выбранного адреса
- Отображение типа и статуса системы
- Обработка ситуации, когда нет зарегистрированных систем

**API интеграция:**
- `addressesApi.getSystems(addressId)` — получить системы адреса

### 3. `DefectTypeSelector.tsx` (новый компонент)
**Путь:** `portal/src/components/DefectTypeSelector.tsx`

**Функциональность:**
- Выбор типа неисправности из списка
- Возможность добавления нового типа (для всех пользователей)
- Удаление типов (для админов)
- Загрузка из системных настроек через React Query

**API интеграция:**
- `useDefectTypes()` — получить все типы
- `useAddDefectType()` — добавить новый тип
- `useDeleteDefectType()` — удалить тип

### 4. `TaskFormPage.tsx` (переделан)
**Путь:** `portal/src/pages/TaskFormPage.tsx`

**Изменения:**
- ❌ Удалены: система шаблонов (TEMPLATE_STORAGE_KEY, applyTemplate, saveTemplate, deleteTemplate)
- ✅ Добавлены: новые компоненты (AddressForm, SystemSelector, DefectTypeSelector)
- ✅ Новая структура данных: `AddressFormData` для адреса
- ✅ Новая валидация для каждого шага
- ✅ Название заявки генерируется автоматически: `{улица}, дом {номер}`

**Поток формы:**
1. AddressForm — вводим адрес
2. SystemSelector — выбираем систему
3. DefectTypeSelector — выбираем тип неисправности
4. Остальные поля (описание, контакты, приоритет, исполнитель, дата)

### 5. `useSettings.ts` (новый хук)
**Путь:** `portal/src/hooks/useSettings.ts`

**Функциональность:**
- `useSettings()` — получить все системные настройки
- `useSetting(key)` — получить одну настройку
- `useDefectTypes()` — получить типы неисправностей
- `useAddDefectType()` — добавить тип
- `useDeleteDefectType()` — удалить тип

**React Query интеграция:**
- Кеширование с TTL 5 минут
- Автоматическое инвалидирование при изменениях

---

## 🖥️ API сервера (FastAPI)

### `system_settings.py` — новые endpoints

#### 1. GET `/api/admin/settings/defect-types/list`
**Доступ:** Все пользователи  
**Ответ:**
```json
[
  {
    "id": "1",
    "name": "Замена фильтра",
    "description": null
  },
  {
    "id": "2",
    "name": "Замена счётчика",
    "description": null
  }
]
```

#### 2. POST `/api/admin/settings/defect-types/add`
**Доступ:** Только админы  
**Запрос:**
```json
{
  "name": "Новый тип",
  "description": "Опциональное описание"
}
```
**Ответ:** `DefectTypeResponse`

#### 3. DELETE `/api/admin/settings/defect-types/{defect_type_id}`
**Доступ:** Только админы  
**Ответ:**
```json
{
  "status": "deleted"
}
```

### `models/settings.py` — инициализация

Добавлена настройка `defect_types` в `init_default_settings()`:
```python
{
    "key": "defect_types",
    "value": '[{"id": "1", "name": "Замена фильтра", ...}]',
    "value_type": "json",
    "group": "interface",
    "label": "Типы неисправностей"
}
```

**Стандартные типы:**
1. Замена фильтра
2. Замена счётчика
3. Ремонт поломки
4. Обслуживание
5. Консультация

---

## 📊 Структура данных

### AddressFormData (Frontend)
```typescript
interface AddressFormData {
  city: string
  street: string
  building: string
  corpus: string
  entrance: string
}
```

### TaskFormData (Frontend)
```typescript
interface TaskFormData {
  address: AddressFormData
  system_id: number | string
  defect_type_id: string
  description: string
  customer_name: string
  customer_phone: string
  priority: TaskPriority
  assigned_user_id: string
  planned_date: string
  photos: File[]
}
```

### DefectType (Backend & Frontend)
```python
# Backend
{
    "id": "uuid-string",
    "name": "Название типа",
    "description": "опциональное описание"
}
```

---

## 🔄 Поток создания заявки

```
1. Пользователь откры вает форму создания заявки
   ↓
2. AddressForm — ввод адреса пошагово
   - Выбор города (автодополнение)
   - Выбор улицы (автодополнение)
   - Выбор дома (автодополнение)
   - Опционально: корпус, подъезд
   ↓
3. SystemSelector — выбор системы
   - Загруженные системы для этого адреса
   - Отображение типа и статуса
   ↓
4. DefectTypeSelector — выбор неисправности
   - Список типов из системных настроек
   - Возможность добавить новый тип
   ↓
5. Заполнение остальных полей
   - Описание проблемы
   - Контакты клиента
   - Приоритет
   - Исполнитель
   - Дата выполнения
   ↓
6. Нажать "Создать заявку"
   - Валидация всех полей
   - Генерация названия: "{улица}, дом {номер}"
   - POST /api/tasks
   - Переход на страницу заявки
```

---

## 🛠️ Миграция / Обновление

### Для существующих пользователей:
1. ✅ Шаблоны **не удаляются** из localStorage (просто не используются)
2. ✅ Старые заявки остаются без изменений
3. ✅ API остается совместимым

### База данных:
- Нет новых таблиц
- Новая настройка добавляется автоматически при инициализации
- Миграция не требуется

---

## ⚠️ Известные ограничения / TODO

1. **Корпусы и подъезды** — пока загружаются статически, нужна реализация API
2. **ID адреса** — SystemSelector требует ID адреса из БД (сейчас undefined)
3. **Фото** — компонент DefectTypeSelector не интегрирован с загрузкой фото

### Для завершения:

- [ ] API для получения корпусов: `GET /api/addresses/{id}/corpuses`
- [ ] API для получения подъездов: `GET /api/addresses/{id}/entrances`
- [ ] Получить ID адреса после выбора через AddressForm (нужен поиск по полному адресу)
- [ ] Интеграция загрузки фото в DefectTypeSelector
- [ ] Добавить истории заявок на адресе в SystemSelector
- [ ] Admin страница для управления типами неисправностей

---

## 📝 Тестирование

### Сценарии:
1. ✅ Создание новой заявки с пошаговым вводом
2. ✅ Добавление нового типа неисправности
3. ✅ Удаление типа неисправности (админ)
4. ✅ Валидация обязательных полей
5. ❓ Получение корпусов/подъездов для адреса

---

## 📚 Файлы, измененные / созданные

### Созданы:
- ✅ `portal/src/components/AddressForm.tsx`
- ✅ `portal/src/components/SystemSelector.tsx`
- ✅ `portal/src/components/DefectTypeSelector.tsx`
- ✅ `portal/src/hooks/useSettings.ts`

### Изменены:
- ✅ `portal/src/pages/TaskFormPage.tsx` (удалены шаблоны, добавлены новые компоненты)
- ✅ `server/app/api/system_settings.py` (добавлены endpoints для типов)
- ✅ `server/app/models/settings.py` (добавлена инициализация типов)

---

## 🔗 Связанные документы

- [CHANGELOG.md](../CHANGELOG.md) — история версий
- [AGENTS.md](../AGENTS.md) — инструкции для AI
- [.github/copilot-instructions.md](.github/copilot-instructions.md) — инструкции для Copilot

---

**Последнее обновление:** 14 января 2026  
**Версия:** 2.4.1
