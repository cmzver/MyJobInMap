# 🧪 Тестирование новой формы заявок

**Статус:** ✅ Готово к тестированию  
**Дата:** 14 января 2026

---

## 🎯 Сценарии тестирования

### ✅ Тест 1: Создание заявки с пошаговым вводом

**Шаги:**
1. Перейти на `http://localhost:5173/portal/tasks/new`
2. Заполнить адрес:
   - Ввести "Москва" в поле "Город"
   - Выбрать "Москва" из выпадающего списка
   - Ввести "Ленина" в поле "Улица"
   - Выбрать "ул. Ленина" из списка
   - Ввести "42" в поле "Дом"
   - Выбрать "42" из списка
3. Проверить, что полный адрес отображается внизу
4. Нажать "Далее" (или скролл вниз)

**Ожидаемый результат:**
- Город, улица, дом заполняются корректно
- Автодополнение работает
- Полный адрес собирается: "Москва, ул. Ленина, 42"

---

### ✅ Тест 2: Выбор системы

**Шаги:**
1. После заполнения адреса
2. Переключиться на карточку "Система обслуживания"
3. Если адрес найден в БД, должны отобразиться системы:
   - Видеонаблюдение
   - Домофония
   - и т.д.
4. Выбрать одну из систем

**Ожидаемый результат:**
- Если системы есть → показывается список
- Если нет → сообщение "На этом адресе нет систем"
- Выбранная система подсвечена

---

### ✅ Тест 3: Выбор типа неисправности

**Шаги:**
1. После выбора системы
2. Переключиться на карточку "Тип неисправности"
3. Должны отобразиться типы:
   - Замена фильтра
   - Замена счётчика
   - Ремонт поломки
   - Обслуживание
   - Консультация
4. Выбрать один из типов

**Ожидаемый результат:**
- Список типов загружается из API
- Выбранный тип отмечен радиокнопкой

---

### ✅ Тест 4: Добавление нового типа неисправности

**Шаги:**
1. На странице "Тип неисправности"
2. Нажать кнопку "Добавить новый тип"
3. Ввести название: "Замена труб"
4. Нажать "Добавить"

**Ожидаемый результат:**
- Новый тип появляется в списке
- Форма очищается
- Новый тип можно выбрать

---

### ✅ Тест 5: Удаление типа неисправности (для админов)

**Шаги:**
1. Залогиниться как админ
2. На странице "Тип неисправности"
3. Наведите на один из типов
4. Нажать иконку "🗑️ Удалить"
5. Подтвердить "Да"

**Ожидаемый результат:**
- Тип удаляется из списка
- Если был выбран → выделение снимается
- На странице типов тип больше не отображается

---

### ✅ Тест 6: Валидация формы

**Шаги:**
1. На странице `/tasks/new`
2. Нажать "Создать заявку" без заполнения данных
3. Проверить, что появляются ошибки:
   - "Город обязателен"
   - "Улица обязательна"
   - "Дом обязателен"
   - "Выберите систему"
   - "Выберите тип неисправности"
   - "Описание обязательно"

**Ожидаемый результат:**
- Красные сообщения об ошибке под полями
- Форма не отправляется

---

### ✅ Тест 7: Полное заполнение формы

**Шаги:**
1. Заполнить все необходимые поля:
   - Адрес (город, улица, дом)
   - Система
   - Тип неисправности
   - Описание: "Требуется замена"
   - Имя клиента: "Иван Петров"
   - Телефон: "+7 900 123-45-67"
   - Приоритет: "Срочная"
   - Исполнитель: выбрать из списка
   - Дата: выбрать дату
2. Нажать "Создать заявку"

**Ожидаемый результат:**
- Заявка создается
- Перенаправление на страницу заявки `/tasks/{id}`
- Toast: "Заявка создана"
- Все данные сохранились корректно

---

### ✅ Тест 8: Редактирование заявки

**Шаги:**
1. Открыть существующую заявку
2. Нажать "Редактировать"
3. Изменить описание
4. Выбрать другого исполнителя
5. Нажать "Сохранить"

**Ожидаемый результат:**
- Форма заполняется текущими данными (где возможно)
- Изменения сохраняются
- Toast: "Заявка обновлена"

---

### ✅ Тест 9: Отмена редактирования

**Шаги:**
1. На странице формы заявки
2. Нажать "Отмена"

**Ожидаемый результат:**
- Возврат на список заявок или страницу заявки
- Изменения не сохраняются

---

### ✅ Тест 10: Адаптивность на мобильном

**Шаги:**
1. Открыть форму на мобильном устройстве (F12 → Device)
2. Протестировать пошаговый ввод адреса
3. Проверить, что выпадающие списки открываются корректно

**Ожидаемый результат:**
- Форма отображается корректно
- Все элементы доступны и нажимаемы

---

## 🐛 Проверка ошибок

### На сервере:

```bash
# 1. Запустить сервер
cd server
make run-server

# 2. Проверить логи
# Ищем ошибки типа:
# - "defect_types setting not found"
# - "Error in DefectTypeSelector"
```

### В браузере:

```javascript
// Открыть консоль (F12 → Console)
// Проверить на ошибки:
// - Network errors (красные запросы)
// - JS errors
```

---

## 📋 Чек-лист тестирования

- [ ] Автодополнение адреса работает
- [ ] Системы загружаются для адреса
- [ ] Типы неисправностей загружаются
- [ ] Можно добавить новый тип
- [ ] Можно удалить тип (админ)
- [ ] Валидация работает
- [ ] Заявка создается
- [ ] Данные сохраняются корректно
- [ ] Редактирование работает
- [ ] На мобильном нормально
- [ ] Нет ошибок в консоли
- [ ] Нет ошибок на сервере

---

## 🔍 API Debugging

### Проверить типы неисправностей:

```bash
curl http://localhost:8001/api/admin/settings/defect-types/list
```

**Ожидаемый ответ:**
```json
[
  {"id": "1", "name": "Замена фильтра", "description": null},
  {"id": "2", "name": "Замена счётчика", "description": null},
  ...
]
```

### Добавить тип:

```bash
curl -X POST http://localhost:8001/api/admin/settings/defect-types/add \
  -H "Authorization: Bearer TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "Тестовый тип"}'
```

### Удалить тип:

```bash
curl -X DELETE http://localhost:8001/api/admin/settings/defect-types/{id} \
  -H "Authorization: Bearer TOKEN"
```

---

## 💡 Советы

1. **Кеш браузера** — если не видите изменения, очистите кеш (Ctrl+Shift+Delete)
2. **Hot reload** — Portal обновляется автоматически при изменении файлов
3. **React DevTools** — используйте для отладки состояния компонентов
4. **Redux DevTools** — для отладки Zustand хранилища
5. **Network tab** — проверьте все запросы, убедитесь что они успешны (200)

---

**Вопросы?** Обратитесь к IMPLEMENTATION.md для подробной информации.

**Версия:** 2.4.1  
**Статус:** ✅ Готово к тестированию
