[tool.isort]
# Keep isort output compatible with black so CI's black --check and
# isort --check-only don't fight over import formatting.
profile = "black"

[tool.black]
line-length = 88
target-version = ["py311"]
