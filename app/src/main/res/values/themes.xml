<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme -->
    <style name="Theme.FieldWorker" parent="android:Theme.Material.Light.NoActionBar" />
    
    <!-- Splash Screen theme -->
    <style name="Theme.FieldWorker.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/Theme.FieldWorker</item>
    </style>
</resources>
